# External IP addresses

## 種類

+ region
+ global

## region

```
WIP
```

## global

```
gcloud beta compute addresses create mix-ip-addr \
    --ip-version=IPV4 \
    --global \
    --project ${_gcp_pj_id}
```
